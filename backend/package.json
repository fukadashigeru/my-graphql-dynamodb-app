{
  "name": "graphql-dynamodb-backend",
  "version": "1.0.0",
  "description": "GraphQL API with DynamoDB Local",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test:connection": "npx ts-node scripts/testConnection.ts",
    "create:table": "npx ts-node scripts/createTable.ts",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "docker:clean": "docker-compose down -v --remove-orphans",
    "setup": "npm run docker:up && sleep 3 && npm run create-table"
  },
  "keywords": [
    "graphql",
    "dynamodb",
    "typescript"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@apollo/server": "^4.9.5",
    "@aws-sdk/client-dynamodb": "^3.450.0",
    "@aws-sdk/lib-dynamodb": "^3.450.0",
    "aws-sdk": "^2.1692.0",
    "dotenv": "^16.3.1",
    "graphql": "^16.8.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/uuid": "^9.0.7",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.0"
  }
}
